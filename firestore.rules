rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Acceso abierto — la seguridad se gestiona por PIN en la aplicación.
    // Suficiente para un sistema de coordinación electoral de mesa.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
